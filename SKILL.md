---
name: novel-writing
description: "完整小说创作流水线：拆书分析→世界观→大纲→细纲→写作→审查→润色→交付。支持Hermes+agy双模型交叉验证，全自动后台推进。"
version: 2.1.0
author: User + Hermes
license: MIT
platforms: [linux]
metadata:
  hermes:
    tags: [novel, fiction, writing, worldbuilding, creative, xianxia, cultivation, autonomous]
    related_skills: [humanizer, ideation, delegate_task]
---

# Novel Writing — 完整小说创作流水线

从拆书分析到交付，8个阶段完整覆盖。支持两种模式：
- **手动模式**：逐步确认，适合新项目或需要调整时
- **自动模式**：一键启动，后台静默推进（原novel-autopilot）

## 用户偏好

- **语言**：中文（所有小说内容）
- **主题**：打工人共鸣、系统性压迫、阶层流动
- **风格**：日常轻松幽默，战斗紧张残酷，强烈反差
- **质量**：每章≥3000中文字符（项目自定义，可在细纲中指定范围。skill默认5000，但项目可覆盖）
- **格式排版**：在飞书消息中避免使用 Markdown 表格，排版在消息栏里显示很乱，应用列表或分段代替。
- **历史真实性**：在处理涉及多时间节点的现实世界数据调研任务（如金价、股价、财报、技术参数历史）时，必须将当前系统实际时间（当前为2026年）与用户给出的历史时间做显式区隔校验，严禁将两个时间的数据混淆。
- **工作流偏好**：对于高风险的底层改动或操作，采用方案先行机制（Plan-first），先给出方案并确认后再动手。
- **学习偏好**：学习类任务以步骤可视化（如看板、卡片、地图等列表）的形式进行展示与进度反馈。
- **调研风格**：偏好官方第一手权威源或确实数据（如上海黄金交易所SGE公报、财报、API），拒绝二手转载，喜欢多版本并行展示对比。
- **基本原则**：不道德说教，快速执行或直接回答可行性，不做无谓防卫或指责。
- **质量**：确保对于重要的事实性数据进行多信源交叉验证后输出。
- **高潮与节奏**：每章结尾有钩子；每5章小高潮；每15章大高潮
- **一致性**：±10章逻辑连贯

## 项目结构

```
~/novel/<project-id>/
├── README.md
├── 大纲/
│   ├── 世界观总纲_v2.md        # 世界观（最终版）
│   ├── 章节大纲_v1.md          # 总纲（100+章框架）
│   ├── 细纲/                    # [阶段2] 详细章节大纲
│   │   ├── 卷01-第001-010章.md
│   │   ├── 卷01-审查报告.md
│   │   └── ...
│   ├── 力量体系/
│   ├── 势力设定/
│   └── 章纲/
├── 正文/
│   ├── 第001章-标题.md          # 3位数字零填充
│   ├── ...
│   └── 审查/                    # [阶段5-7] 审查报告
│       ├── 第001章-审查.md
│       ├── 段落01-审查.md
│       └── 卷01-终审.md
├── 参考素材/                    # [阶段0] 拆书分析
│   └── <作品名>/
│       ├── 拆书分析.md
│       └── ...
├── 地缘格局/
├── 伏笔追踪/
├── 风格/
└── .git/
```

## 全自动流水线（8个阶段）

### 启动方式

用户说"写第X-Y章"或"继续推进"或"写下一卷"或"把这个大纲进入Skill流程"或"生成卷X的内容"，即触发流水线。
收到后**不再询问，直接按以下阶段依次执行**。

### 项目初始化（首次使用时）

如果是新项目，需要先进行初始化：

```bash
# 执行自动化初始化命令
python3 ~/.hermes/skills/creative/novel-writing/scripts/novel_pipeline.py --dir ~/novel/<project-id> init

# 初始化 git 仓库并进行首次 commit
cd ~/novel/<project-id>
git init
git add -A
git commit -m "feat(init): 项目初始化"
```

---

### 阶段 0：参考素材拆书分析

**何时执行**：
- 用户指定参考作品时 → 执行
- 用户说「写第X章」但没提参考作品 → 跳过，直接进入阶段1
- 新项目启动且有参考作品 → 执行

**做什么**：
1. 读取参考作品的前50-100章
2. 从以下维度拆解：
   - **剧情推进方式**：驱动力是什么？（资源压力/生存威胁/信息差）
   - **引子系统**：长线/中线/短线伏笔如何设计？
   - **钩子设计**：每章/每阶段结尾的钩子类型
   - **战力体系**：「以弱胜强」如何处理才合理？
   - **动机共鸣**：读者为什么会认同主角的行为？
3. 总结可迁移的核心技巧

**分工**：Hermes做拆书结构分析，agy做创意技巧总结

**输出**：`参考素材/<作品名>/拆书分析.md`

**为什么重要**：避免「动机共鸣断裂」和「战力体系崩塌」

---

### 阶段 1：世界观搭建

**输入**：用户的核心设定 + 拆书分析（如有）

**做什么**：
1. 用户提供核心前提（如"散修=打工人"、"宗门明暗账"）
2. 双模型并行构建：
   - Hermes：结构分析、逻辑一致性
   - agy：创意扩展、不同视角
3. 合并最优版本
4. 逻辑自洽检查：
   - 资源系统（什么稀缺？谁控制？）
   - 力量层级（修为等级、社会阶层）
   - 知识传递（信息如何流动？什么被垄断？）
   - 经济系统（货币、市场、劳动力）
   - 冲突来源（战争、势力、外部威胁）

**输出**：`大纲/世界观总纲_v2.md`

**并行模型协作**：

| 任务 | Hermes角色 | agy角色 |
|------|-----------|---------|
| 世界观 | 结构、逻辑、一致性 | 创意扩展、备选方案 |
| 大纲 | 框架、节奏 | 细节、情绪节拍 |
| 正文 | 审查、质量控制 | 主力写作 |

---

### 阶段 2：大纲设计

**输入**：世界观 + 总纲框架

**做什么**：
1. **卷结构规划**：把大段切分成10-30章的「小情节段」
   - 切分依据：情节转折点、新角色登场、场景转换、情绪节奏
2. **细纲编写**：每章包含——
   - 章节标题
   - POV（视角人物）
   - 场景
   - 核心事件（1-3件事）
   - 情绪曲线（起→承→转→爽/虐）
   - 章末钩子
   - 伏笔标注（新埋/回收）
   - 预估字数
3. 确保段与段之间衔接流畅
4. 确保伏笔有规划

**输出**：
- `大纲/细纲/卷XX-结构规划.md`
- `大纲/细纲/卷XX-第YYY-ZZZ章.md`

**Git commit**：`feat(outline): 卷XX 细纲`

---

### 阶段 3：大纲审查

**输入**：细纲 + 世界观 + 已有正文

**审查维度（7项必查）**：

| 维度 | 检查内容 |
|------|---------|
| 逻辑自洽 | 与世界观是否矛盾？前后因果链是否通顺？ |
| 情绪节奏 | 是否有张弛？连续多少章同一种情绪？钩子密度是否合理？ |
| 剧情推进 | 每段是否有实质进展？是否有"水"章节？ |
| 人物一致性 | 角色行为是否符合设定？性格是否突变？ |
| 伏笔管理 | 埋设的伏笔是否有回收计划？已埋伏笔是否遗忘？ |
| 爽点分布 | 每5章是否有小高潮？每15章是否有大高潮？ |
| 战力合理性 | 「以弱胜强」是否有足够铺垫？战力体系是否崩塌？ |

**审查流程**：
1. 自审：基于以上维度逐项检查
2. 交叉审：委托agy审查，context中必须包含世界观和细纲
3. 合并：修正所有问题

**门禁**：A级问题（逻辑硬伤）修正后重新审查；B级问题标注后继续

**输出**：`大纲/细纲/卷XX-审查报告.md`

**Git commit**：`review(outline): 卷XX 细纲审查通过`

---

### 阶段 4：逐章写作

**输入**：细纲（通过审查）+ 世界观 + 前面章节正文 (+ 数值台账)

**微观机制与约束（吸收 open-novel-fanqie 核心思想）**：
1. **情节拍微分生成 (Micro-Beats)**：
   - 严禁让 AI 一口气输出整章正文。必须将本章细纲切分为 2-4 个具体的“情节节拍（Beats）”并作为独立步骤逐步演进。
   - 连贯步进：一次只让 AI 扩写“一拍细纲”(产出 1000-1500 字)，通过校验并衔接完成后，再进入下一拍，最后强行拼装成 4000-5000 字以上的高品质自然章。
2. **数值及消耗账目化 (Ledger Control)**：
   - 只要涉及设定中带有负荷、冷却或资源金币的金手指能力（如“大脑效率优化器”），每拍写作中必须严格扣减对应数值（灵力百分比、寿命、灵石、体力值）。
   - 让人物必须精打细算、规避责任并在此后反映出“冷却期脱力、眩晕、或因钱不够无法动用外挂”的硬性代价，绝不能凭空爆种或使用无限制金手指。
3. **严格的第三人称限制视角 (POVL-3)**：
   - 不允许任何超越主角（陆承舟/陈渊）感官局限的上帝天眼旁白（例如 “*他还不知道，接下来的一步是个陷阱*” ❌）。
   - 旁白反穿透：绝对禁止直接宣告配角的脑内活动（例如 “*执事暗自咬牙*” ❌）。必须通过动作细节、微表情或客观物件侧面暗示（例如 “*执事嘴角颤了颤，攥着账册的手指蓦然捏得发白*” ✔️）。
   - 去 AI 腔调化：高频走查并禁用“不禁”、“嘴角微微上扬”、“逆天改命”、“说教感”、“修仙界残酷的法则”。

**基础要求**：
- 每章≥5000中文字符（用`wc -m`验证）
- 遵循起承转爽节奏
- 章末有钩子
- 与前文衔接自然

**委托与细节生成模式**（防灌水与AI偷懒铁律）：
- **拍（Beats/情景）级细分生成**：严禁直接将整章细纲直接扔给 AI 一次性生成整章。必须将每章细纲进一步切分为 **2-4 个微观的“情节拍/微场景（Beats）”**。让 AI **一拍一拍地生成**段落正文（每拍生成 1000-1500 字），写完后逐一校验并拼接。这能彻底解决 AI 越过细节、快速交代剧情、灌水和节奏坍塌的问题。
- 每批**3章**（5章会触发迭代上限）
- 每批必须读前1-2章作为风格锚点
- 写完每章立即`wc -m`验证字数

**金手指与数值的数学“账目化”约束**：
- **硬性数值扣减**：对于主角拥有的金手指（如“效率优化器”等）或当前的灵力、库存资源量，每次在正文中发生动用时，必须在底层的 `剧情状态.md` 或 `数值台账.md` 中进行显式且精准的数学运算和记录，并在本章的写作 context 中强制塞入当前的 **[金手指/数值变动限制限制]**。
- 例如：如果本章主角用了一次推演（花费 3 块灵石，灵力耗尽过载 6 小时），在接下来的 2 拍里，主角必须在正文中明确表现出虚脱无力、并且严禁在冷却期内再次使用金手指。

**防止越界与AI味清洗（第三人称限制视角）**：
- **第三人称严格限制视角**：只能描写主角看得到、听得到、能接触的客观事实以及主角的内心活动。**严禁**随意越过主角的头脑去描写他人的内心感受（如 "路人心中无比震惊" ✗）、**严禁**跳出视角的全局全知旁白剧透或说教式感慨（如 "他却不知道，这一次选择将改变一切" ✗）。
- **人设说教与网络梗双规避**：严格禁止出现网络流行语（“卧槽”、“给力”等）、现代流行梗或说教式的大道理宣讲。

**Context模板**：
```
### 风格要求（必须严格遵守）
- 第三人称限制视角
- [项目特定的风格描述]
- 每章≥[N]中文字符（用 wc -m 验证）。N值取自细纲中的"预估字数"下限
- 绝对禁止：[列出]
- 鼓励：[列出]

### 样本风格（必须模仿这个语调）
> [从已写正文中选取1-2段作为风格锚点]

### 角色语言指纹
- 角色A：[语言特征描述]
- 角色B：[语言特征描述]
- 未命名角色（如[师傅]）：此角色暂未命名，不要自行取名

### 前情提要（最近3-5章的简要概述）
[用3-5句话概括前文，包含关键事件和当前状态]

### 本批章节细纲
[每章的细纲，必须包含：场景、核心事件（带编号）、情绪曲线、章末钩子、伏笔标注]

### 写完后必须
1. 每章单独保存为一个文件
2. wc -m 验证每章字数
3. 保存路径和文件命名
- **语言约束**：所有输出使用中文，标点符号使用中文全角（，。！？；：""''）
- **工具约束**：修改已有章节时使用 `patch` 工具（局部修改），不要用 `write_file`（整文件重写）
```

**实测有效的委托prompt结构**（比模板更具体）：
- 先给风格要求（3-5条硬规则）
- 再给1-2段样本文字（从已写正文中选）
- 再给角色语言指纹（每个角色2-3行）
- 再给前情提要（3-5句话）
- 再给每章细纲（场景→核心事件→情绪曲线→章末钩子→伏笔）
- 最后写完后要求（保存路径+字数验证）

**输出**：`正文/第XXX章-标题.md`

**Git commit**：每5章commit一次，`feat(chapter): 第X-Y章正文`

---

**自动校验层**：
在进行人工/LLM质检前，必须首先运行自动化脚本检查字数、编码、格式和逻辑冲突：
```bash
python3 ~/.hermes/skills/creative/novel-writing/scripts/novel_pipeline.py --dir ~/novel/<project-id> validate --file 正文/第XXX章-标题.md
```
若该脚本返回错误，必须根据错误日志打回修改，直到通过校验。

**审查内容**：

| 维度 | 检查内容 |
|------|---------|
| 字数 | ≥5000中文字符 (通过 validate 脚本检查) |
| 逻辑 | 与前文有无矛盾？与细纲是否一致？ |
| 节奏 | 起承转爽结构是否完整？ |
| 钩子 | 章末是否有有效钩子？ |
| 风格 | 幽默感、打工人梗、修仙术语是否一致？ |
| 衔接 | 与上一章的过渡是否自然？ |

**修正**：如有问题，直接修改该章，不重新写。修完再审一次。

**输出**：`正文/审查/第XXX章-审查.md`（简要记录，不交付给用户）

---

### 阶段 6：分段审查

**触发条件**：一个小情节段的所有章节写完（如第51-55章全部完成）

**审查维度**：

| 维度 | 检查内容 |
|------|---------|
| 段落完整性 | 这一段的情节是否闭合？有没有悬而未决的线？ |
| 情绪曲线 | 整段的情绪起伏是否合理？ |
| 伏笔 | 本段埋的伏笔是否标注？与全局伏笔表是否一致？ |
| 人物弧光 | 本段涉及的角色是否有变化/成长？ |
| 节奏 | 段内各章节奏是否协调？ |
| 衔接 | 与上一段的过渡是否自然？ |
| 战力体系 | 「以弱胜强」是否有合理铺垫？ |

**高频A级问题（实测）**：

| 问题类型 | 表现 | 修复方式 |
|---------|------|---------|
| 角色名不一致 | 不同批次给同一角色不同名字 | 统一到细纲设定 |
| 时间线倒流 | 章节内容时间顺序与编号不符 | 修改时间描述 |
| 设定矛盾 | 宿舍人数、地点名称等前后不一 | 以细纲为准 |
| 重复段落 | 迭代扩充时复制粘贴导致 | 删除重复段 |
| 动机共鸣断裂 | 读者无法理解主角行为的动机 | 补充铺垫或调整设定 |
| 战力体系崩塌 | 以弱胜强缺乏合理解释 | 补充智谋/信息差/道具铺垫 |

**输出**：`正文/审查/段落XX-审查.md`

---

### 阶段 7：风格润色

**触发条件**：用户要求润色，或整卷终审后发现风格问题

**做什么**：
1. 与参考作品对比（8维度）：
   1. 节奏控制 — 紧张/舒缓循环、高潮位置
   2. 伏笔埋设 — 密度、联动、回收时机
   3. 人物塑造 — 行动vs说明、细节暗示
   4. 环境描写 — 感官细节密度、情绪服务
   5. 对话质量 — 潜台词、简洁度、人物声音
   6. 心理描写 — 克制度、留白vs解释
   7. 爽点设计 — 认知碾压、资源收割
   8. 文笔质感 — 精炼度、原创性、重复
2. 生成「原文→润色后」对比示例
3. 按20-30%幅度修改（用patch，不要整文件重写）

**备份**：
```bash
mkdir -p 正文_原始备份
cp 正文/第0XX章-*.md 正文_原始备份/
```

**输出**：`风格对比与润色建议.md` + 修改后的章节

---

### 阶段 8：整卷终审 + 交付

**触发条件**：一卷所有章节写完并审查通过

**终审内容**：
1. 通读全卷，检查整体节奏
2. 检查伏笔的埋设/回收是否完整
3. 检查角色发展弧线
4. 检查世界观一致性
5. 统计总字数

**输出**：
- `正文/审查/卷XX-终审.md`
- Git commit：`feat(chapter): 卷XX 完成（N章，约X万字）`

**交付通知**：
- 向用户发送交付报告：卷名、章数、总字数、关键剧情节点
- 附上文件路径列表
- 附上终审报告摘要（如有B级问题需用户知晓）

**风格润色**（阶段7）：如果终审发现风格问题，触发风格润色流程

**Google Docs交付**（可选）：
```bash
# 刷新token
python3 -c "
import json, requests
with open('$HOME/.hermes/google_token.json') as f:
    t = json.load(f)
r = requests.post('https://oauth2.googleapis.com/token', data={
    'client_id': t['client_id'],
    'client_secret': t['client_secret'],
    'refresh_token': t['refresh_token'],
    'grant_type': 'refresh_token'
})
d = r.json()
if 'access_token' in d:
    t['token'] = d['access_token']
    with open('$HOME/.hermes/google_token.json', 'w') as f:
        json.dump(t, f)
"

# 上传markdown为Google Doc
TOKEN=$(python3 -c "import json; print(json.load(open('$HOME/.hermes/google_token.json'))['token'])")
curl -s -X POST \
  "https://www.googleapis.com/upload/drive/v3/files?uploadType=multipart" \
  -H "Authorization: Bearer $TOKEN" \
  -F 'metadata={"name": "标题", "mimeType": "application/vnd.google-apps.document"};type=application/json;charset=UTF-8' \
  -F "file=@/path/to/file.md;type=text/markdown"

# 设置权限
curl -s -X POST "https://www.googleapis.com/drive/v3/files/$DOC_ID/permissions" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"role": "reader", "type": "anyone"}'
```

---

## 自动推进机制

### 不询问原则

以下情况**不询问用户**，直接执行：
- 细纲审查发现B级问题 → 修正后继续
- 单章审查发现风格/逻辑问题 → 修改后继续
- 段落审查发现节奏问题 → 调整后继续

### 需要通知用户的情况

- 细纲审查发现**A级逻辑硬伤**（与核心设定冲突）→ 通知用户，附上分析和建议方案
- 写作过程中发现**大纲需要调整**（原有情节不合理）→ 通知用户，说明原因
- 整卷交付时 → 通知用户

### A级/B级问题判定标准

| 级别 | 定义 | 典型场景 |
|------|------|---------|
| A级 | 逻辑硬伤，必须修正 | 时间线倒流、角色名不一致、设定矛盾、战力体系崩塌、动机共鸣断裂 |
| B级 | 建议修改，可标注后继续 | 连续3章无钩子、情绪节奏平缓、字数略不足、风格轻微漂移 |
| C级 | 小瑕疵，记录即可 | 标点符号错误、个别用词不当、格式小问题 |

### 与agy的协作模式

- **阶段0-2（拆书/世界观/大纲）**：Hermes写初稿 → agy交叉审查 → 合并最优版
- **阶段3（大纲审查）**：Hermes主审 → agy复核关键维度
- **阶段4-5（正文/逐章审）**：agy写正文 → Hermes审查质量
- **阶段6-7（分段审/润色）**：Hermes主审 → agy复核
- **阶段8（终审交付）**：Hermes主审 → agy复核
- **剧情状态及辅助文档维护**：agy写后更新相关字段，Hermes在审查时校验并补充其完整性

**争议仲裁机制**：当Hermes和agy对某个修改有分歧时，以Hermes的判断为准（因为Hermes是用户的直接代理，更了解用户偏好），但agy的不同意见应记录在审查报告中。

**agy中文能力容错**：
- 在Context模板中增加修仙术语表（如：筑基=突破到某个境界，金丹=更高境界）
- 如果agy对中文修仙术语理解不准确，Hermes应在审查时主动修正

核心原则：**一个写，一个审**。

---

## 阶段 4b：风格优化（已有正文的风格重写）

**何时执行**：用户说"优化风格""重写""太干巴巴了"等。

**与阶段7（润色）的区别**：阶段7是微调（20-30%修改），阶段4b是结构性风格重写（可能改50%+）。

### 工作流：诊断 → 方案 → 实施

1. **诊断阶段**：读3-5章代表章节 + 风格锚定文档，列出具体问题（不要泛泛而谈）
2. **方案阶段**：给出3-5个具体优化方案，每个附带"原文→优化后"示例。**让用户确认后再执行。**
3. **实施阶段**：创建/更新风格锚定文档 → 备份原文到`正文_原始备份/` → 分批委托重写

### 弹性增强层设计模式

**关键用户偏好**：风格增强手段不要设硬性"每章必加"的指标。用"判断标准"代替"数量要求"。

示例结构（写入风格锚定文档）：
```markdown
### 5.X 手段名称
- 什么时候该加：[判断标准]
- 什么时候不该加：[判断标准]
- 频率参考：每章0-N个（非强制）
```

验证清单拆分为：
- **基础项（必检）**：钩子、节奏、衔接、字数等硬指标
- **弹性增强项（AI判断）**：每个手段一个判断问题，适合就加，不适合跳过

### 分批重写委托

- 备份原文：`mkdir -p 正文_原始备份 && cp 正文/第XXX章-*.md 正文_原始备份/ && git commit`
- 每批2-3章（3章可能超时，600s限制）
- Context必须包含：风格锚定文档路径 + 原文路径 + 修改方向摘要
- **超时处理**：subagent超时后检查文件是否已写入（可能在超时前完成了写入），用`wc -m`验证字数
- 每批完成后`git commit`
- 最终对比：`diff 正文_原始备份/ 正文/` 确认改动范围

---

## 质量标准

### 字数
- 每章≥5000中文字符（`wc -m`统计）
- 日常章节5000-8000字
- 战斗/高潮章节8000-12000字

### 节奏
- 每章结尾必须有钩子
- 连续日常章节不超过3章（需穿插冲突/伏笔）
- 连续战斗章节不超过2章（需穿插喘息/疗伤/谋划）
- 每5章一个小高潮
- 每15章一个大高潮

### 一致性
- 角色名/地名/术语与世界观总纲一致
- 修为等级/力量体系与设定一致
- 时间线不矛盾

### 动机共鸣（关键）
- 每个「以弱胜强」必须有合理铺垫（智谋/信息差/道具/帮手）
- 读者无法理解的行为必须补充铺垫或调整设定
- 战力差距不能靠硬打，要靠脑子打

---

## Pitfalls

1. **Subagent迭代上限**：3章/批是安全阈值，5章/批经常超时。超时后检查已写入文件，从断点继续。

2. **字数验证必须写后即查**：`wc -m`验证，中文字符byte/char比约3:1，不能用文件大小估算。

3. **上下文溢出**：世界观文档可能太大（60K+），委托时引用关键章节而非全文。

4. **风格漂移**：每批写作的subagent都需要读前1-2章正文作为风格锚点。

5. **审查流于形式**：审查prompt必须具体，不能只说"检查逻辑"。

6. **过度审查**：逐章审查要快（1-2分钟），深度审查留给阶段6和7。

7. **大纲与正文脱节**：写作过程中发现细纲不合理时，先修改细纲（commit），再修改正文。

8. **角色名/设定不一致**：细纲中必须包含「角色出场统计表」和「设定一致性清单」。

9. **时间线倒流**：细纲中每章标注明确的时间节点，写作时严格遵循。

10. **重复段落**：分段审查时专门检查「是否有连续2段以上内容高度相似」。

11. **润色过度修正**：agy的润色倾向过于激进（50-60%删减vs目标20-30%）。明确指令「20-30%修改，用patch工具，不要整文件重写」。

12. **用户中途插话**：流水线运行中用户可能发消息。收到用户消息后暂停流水线，响应用户，确认后再继续。

13. **文件编码问题**：中文文件必须使用UTF-8编码，避免GBK/UTF-8编码混乱导致`wc -m`计数不准确。

14. **agy拒绝执行中文任务**：某些agy对非英文输出有偏见或被训练限制拒绝写中文。在Context中明确要求「必须使用中文输出」。

15. **LLM输出截断**：agy写长章节（8000+字符）时可能被上下文窗口截断，导致章节在中间戛然而止。写完后检查章节末尾是否有完整的结尾。

16. **Google Docs交付失败**：token刷新和上传可能失败。降级方案：直接交付Markdown文件。

17. **连续写作疲劳**：自动模式连续运行几十章，中间没有人工审查介入，可能导致后期质量持续下降。建议每10章暂停一次，人工抽查质量。

18. **核心设定中途变更**：用户可能在写到一半时要求更换金手指/核心设定。流程：① 备份当前正文 ② 更新大纲和所有辅助文档（世界观、剧情状态、伏笔追踪等）③ 用sed批量替换旧设定关键词 ④ 清空正文目录 ⑤ 从头重写。**不要保留旧版备份**——用户明确说"直接重写就行了，备份了好几次都不要了"。

19. **Google OAuth token过期**：每次Google Docs上传前必须先刷新token。token在session间会过期，不要假设上一次的token还有效。刷新脚本见阶段8的Google Docs交付部分。

20. **Subagent超时后文件可能已写入**：delegate_task超时（600s）不代表文件没写。subagent可能在超时前完成了write_file但超时在后续验证步骤。超时后用`wc -m`检查文件字数和`tail -5`检查文件完整性，不要盲目重写。3章/批容易超时，建议2章/批。

21. **风格重写必须先诊断再动手**：用户明确要求"不是重写文章，是要方案"时，必须先给出优化方案（附示例），让用户确认后再执行。不要跳过方案直接重写。

22. **弹性增强层 > 硬性指标**：用户明确说"不用必须每章几个，用AI分析哪些适合加的加上就行，不是每章必加"。风格增强手段用"判断标准"代替"数量要求"，验证清单分"基础项"和"弹性增强项"。

23. **Agy审查skill的模式**：定期让agy审查skill本身（读取SKILL.md → 按7维度评价 → 输出P0-P3问题清单）。实测发现32个问题（3个P0阻塞、7个P1高优、9个P2中优、13个P3低优），修复后skill质量显著提升。

24. **Agy自行命名角色冲突**：agy在写作时可能给未正式命名的角色取名（如给"[师傅]"取名"赵志远"），导致不同批次命名不一致。解法：细纲中必须明确标注所有角色的正式名称。如果角色暂未命名（如"[师傅]"），在细纲和委托context中用占位符明确说明"此角色暂未命名，不要自行取名"。已命名的角色在角色语言指纹中列出。

25. **字数目标不一致**：skill默认≥5000，但项目可能自定义为3000-4000。委托context中的字数要求必须与细纲中的"预估字数"一致，不能照搬skill默认值。写完后用wc -m验证，略欠（≥90%目标）可接受，严重不足（<80%）必须补写。

26. **并行建立辅助文档**：阶段4开始前，可以一次性并行创建所有辅助文档（剧情状态.md、角色语言指纹.md、情绪地图.md、伏笔追踪表.md），不需要逐个创建。效率更高，且确保写作开始时所有参考文档都就位。

27. **Agy patch工具的文本匹配问题**：agy在使用patch工具时，如果文件已被其他subagent修改，old_string可能匹配失败。解法：每次delegate_task后检查返回的summary中是否有patch失败的记录，如有则用git status确认文件状态。

---

## 爽点设计6种手法

1. **反差爽** — 弱者碾压强者的极致快意
2. **信息差** — 主角知道别人不知道的信息，利用信息优势获利
3. **打脸爽** — 被嘲笑后证明自己，让嘲笑者后悔
4. **升级爽** — 境界突破、能力提升、装备更新
5. **收服爽** — 强者臣服、势力归附
6. **复仇爽** — 大仇得报、冤屈昭雪

---

## 动态审查机制

### 滑动窗口文档

维护一个动态更新的「剧情状态文档」，每写完一章就更新，每审查一次就检查。

**文档结构**：`大纲/剧情状态.md`

```markdown
# 剧情状态文档（滑动窗口）

## 基础信息（固定）
- 项目名：
- 当前卷：
- 当前章节范围：

## 主角状态（动态更新）
- 姓名：
- 修为：
- 核心能力/金手指：
- 当前位置：
- 所属势力：
- 经济状况：
- 人际关系网：
  - [角色名]：关系描述（最近互动）

## 本段剧情（最近5-10章）
- 当前阶段：[小情节段名称]
- 核心冲突：
- 情绪基调：
- 已解决的问题：
- 未解决的问题：

## 历史剧情精简（更早的章节）
- [阶段1]：一句话概括关键事件
- [阶段2]：一句话概括关键事件
- ...

## 未来伏笔/钩子
- [伏笔1]：埋设章节 → 预计回收章节 → 当前状态
- [伏笔2]：...
- [钩子1]：触发条件 → 预期效果

## 角色出场表
| 角色 | 首次出场 | 最近出场 | 身份 | 状态 |
|------|---------|---------|------|------|
| 主角 | 第1章 | 第X章 | ... | ... |
| 配角A | 第3章 | 第X章 | ... | ... |
```

### 滑动窗口更新规则

1. **每写完一章**：更新「主角状态」「本段剧情」「角色出场表」
2. **每5章或小情节段结束时（以先到者为准）**：将「本段剧情」中最旧的2-3章移入「历史剧情精简」
3. **伏笔回收时**：更新「未来伏笔/钩子」状态

### 每章审查检查节点

写完每章后，必须检查：

| 检查项 | 检查内容 | 文档依据 |
|--------|---------|---------|
| **时间** | 本章时间点是否与前文一致？是否有时间倒流？ | 剧情状态.md → 本段剧情 |
| **地点** | 场景位置是否与设定一致？ | 世界观总纲 |
| **人物** | 出场角色是否与角色出场表一致？名字是否正确？ | 剧情状态.md → 角色出场表 |
| **细纲** | 本章内容是否符合细纲？偏差是否合理？ | 细纲.md |
| **剧情关联** | 与前文的关联度如何？是否有断裂？ | 剧情状态.md → 本段剧情 |
| **推进内容** | 本章推进了什么？解决了什么问题？制造了什么问题？ | 剧情状态.md → 未解决的问题 |
| **情绪引动** | 本章的情绪节拍是什么？读者会有什么感受？ | 细纲.md → 情绪曲线 |
| **实力提升** | 主角实力是否有变化？是否合理？ | 剧情状态.md → 主角状态 |
| **人物关系** | 人物关系是否有变化？是否自然？ | 剧情状态.md → 人际关系网 |
| **伏笔检查** | 本章是否埋了新伏笔？是否回收了旧伏笔？ | 剧情状态.md → 未来伏笔/钩子 |

### 委托agy时的Context模板

每次委托写作时，**必须**包含剧情状态文档：

```
### 剧情状态文档（必读）
[读取 大纲/剧情状态.md 的完整内容]

### 风格要求
...

### 前情提要
[从剧情状态文档的「本段剧情」中提取最近3-5章]

### 本批章节细纲
...

### 写完后必须
1. wc -m 验证字数
2. 更新 大纲/剧情状态.md（主角状态、本段剧情、角色出场表、伏笔）
```

### 审查时的检查流程

**逐章审查**（阶段5）：
1. 读取剧情状态文档
2. 对照本章内容，逐项检查上表
3. 如有问题，修改章节内容
4. 更新剧情状态文档

**分段审查**（阶段6）：
1. 读取剧情状态文档
2. 检查本段所有章节的一致性
3. 检查伏笔的埋设/回收是否与文档一致
4. 压缩历史剧情，更新滑动窗口

---

## 辅助文档体系

除了核心的「剧情状态.md」滑动窗口，还需要维护以下文档：

### 1. 伏笔追踪表 `大纲/伏笔追踪表.md`

```markdown
# 伏笔追踪表

| ID | 伏笔内容 | 埋设章节 | 预计回收 | 实际回收 | 状态 |
|----|---------|---------|---------|---------|------|
| F001 | 主角发现功法被删减 | 第3章 | 第15章 | - | 已埋 |
| F002 | 散修联盟的秘密 | 第10章 | 第30章 | - | 已埋 |
| F003 | 老周头的真实身份 | 第5章 | 第25章 | 第25章 | 已收 |
```

**更新规则**：
- 每章写完后检查：本章是否埋了新伏笔？→ 加入表中
- 每章写完后检查：本章是否回收了旧伏笔？→ 更新状态
- **警告**：伏笔超过预计回收章节还未收 → 标记为「过期」

**审查时检查**：
- 伏笔数量是否过多（>15个未收）？→ 回收压力警告
- 是否有伏笔「遗忘」（超过预计回收2章以上）？→ 立即处理

### 2. 关键物品追踪表 `大纲/关键物品追踪表.md`

追踪功法、法宝、丹药、奇遇等物品的出现/使用/状态变化：

```markdown
# 关键物品追踪表

| ID | 物品名称 | 首次出现 | 最近出现 | 当前状态 | 持有者 |
|----|---------|---------|---------|---------|--------|
| I001 | 效率优化器 | 第1章 | 第X章 | 激活 | 主角 |
| I002 | 残缺功法 | 第1章 | 第X章 | 已补全 | 主角 |
| I003 | 筑基丹 | 第15章 | 第15章 | 已使用 | 主角 |
```

**更新规则**：
- 新物品出现时 → 加入表中
- 物品状态变化时 → 更新状态
- 物品转移持有者时 → 更新持有者

### 2. 角色语言指纹 `大纲/角色语言指纹.md`

防止「遮住名字分不清谁在说话」：

```markdown
# 角色语言指纹

## 主角（陆承舟）
- 口头禅：无
- 说话风格：简洁、务实、少废话
- 思考方式：程序员思维，喜欢拆解问题
- 禁忌：不会说「卧槽」「牛逼」等现代脏话

## 老周头
- 口头禅：「当年我在...」
- 说话风格：吹牛、夸张、爱讲故事
- 特征：经常说到一半被打断

## 孙胖子
- 口头禅：「我跟你说...」
- 说话风格：絮叨、热心、容易紧张
- 特征：紧张时语速变快
```

**委托agy时**：必须包含相关角色的语言指纹，让agy模仿。

### 3. 情绪地图 `大纲/情绪地图.md`

可视化整卷的情绪起伏，防止「连续5章都是平的」：

```markdown
# 情绪地图（第一卷）

| 章节 | 情绪标签 | 钩子类型 | 字数 | 备注 |
|------|---------|---------|------|------|
| 第1章 | 平 | 悬念 | 6200 | 开局 |
| 第2章 | 平→微升 | 新信息 | 5800 | 发现金手指 |
| 第3章 | 升 | 危机 | 7100 | 第一次任务成功 |
| 第4章 | 平 | 悬念 | 5500 | 日常 |
| 第5章 | 平 | 新信息 | 5200 | 日常 |
| 第6章 | 升 | 反转 | 6800 | 炼丹突破 |
| 第7章 | 平→降 | 悬念 | 5900 | 茶馆被拒 |
| 第8章 | 升 | 危机 | 6500 | 接外包任务 |
| 第9章 | 高 | 反转 | 8200 | 血战 |
| 第10章 | 平→升 | 新信息 | 6100 | 联盟线索 |
```

**更新规则**：**每章写完后立即记录**（不是每5章），这样自动触发规则才能实时检测「连续3章情绪为平」

**每5章**：生成可视化曲线（可选），分析情绪节奏

**审查时检查**：
- 是否有「连续3章情绪为平」？→ 节奏警告
- 是否有「连续2章情绪为高」？→ 需要喘息章节
- 每5章是否有小高潮？每15章是否有大高潮？

### 4. 势力状态文档 `大纲/势力状态.md`

追踪势力变化，防止设定矛盾：

```markdown
# 势力状态

## 碧云宗
- 当前态度：对主角中立偏正面
- 内部矛盾：外门vs内门
- 最近动作：邀请主角当客卿
- 关键人物：赵长风（外事长老）、宋远山（长老）

## 散修联盟
- 当前态度：对主角友好
- 内部矛盾：激进派vs温和派
- 最近动作：邀请主角加入
- 关键人物：沈青竹（联络人）、马三（外围成员）

## 万宝楼
- 当前态度：对主角警惕
- 内部矛盾：无明显矛盾
- 最近动作：调查主角的灵草来源
- 关键人物：林远舟（收货员）
```

**更新规则**：势力态度或关键人物变化时更新

### 5. 自动触发规则

像cronjob一样，设定自动触发条件：

| 触发条件 | 动作 | 执行者 |
|---------|------|--------|
| 写完第5章 | 触发「小情节段审查」 | Hermes |
| 写完第15章 | 触发「大高潮检查」 | Hermes |
| 伏笔数量>15个未收 | 触发「伏笔回收压力警告」 | Hermes |
| 连续3章情绪为「平」 | 触发「节奏警告」 | Hermes |
| 主角修为未提升超过10章 | 触发「成长停滞警告」 | Hermes |
| 新角色出场 | 检查角色出场表是否已录入 | agy |
| 角色连续10章未出场 | 检查是否该角色已无用，更新角色出场表 | Hermes |
| 连续2章字数<5000 | 触发「字数警告」，检查是否有意缩短还是质量问题 | Hermes |
| agy写作超时≥2次 | 降级为1章/批 + 通知Hermes手动检查 | Hermes |

---

## 委托agy时的完整Context模板

每次委托写作或审查时，**必须**包含以下文档：

```
### 必读文档（按优先级）
1. 大纲/剧情状态.md — 当前剧情上下文
2. 大纲/角色语言指纹.md — 相关角色的说话风格
3. 大纲/伏笔追踪表.md — 伏笔状态
4. 大纲/情绪地图.md — 情绪节奏参考
5. 大纲/势力状态.md — 势力关系参考

### 风格要求
...

### 前情提要
[从剧情状态文档的「本段剧情」中提取最近3-5章]

### 本批章节细纲
...

### 写完后必须
1. wc -m 验证字数
2. 更新 大纲/剧情状态.md
3. 更新 大纲/伏笔追踪表.md（如有新伏笔或回收）
4. 更新 大纲/情绪地图.md（每章）
5. 更新 大纲/势力状态.md（如有变化）
6. 检查自动触发规则
```

---

## References

- `references/writing-techniques.md` — 爽点设计、节奏控制、钩子设计、情绪共振
- `references/worldbuilding-framework.md` — 世界观构建框架、逻辑自洽检查清单
- `references/chapter-delegation-pattern.md` — 章节批量写作委托模式、质量检查清单
- `references/review-delegation-pattern.md` — 审查委托模板（大纲/逐章/分段）
- `references/segment-review-template.md` — 分段审查模板、高频A级问题
- `references/flexible-enhancement-layer.md` — 弹性增强层设计模式（替代硬性风格指标）
- `references/style-comparison-template.md` — 风格对比分析模板、8维度对比框架
- `templates/style-anchor-v2.md` — 风格锚定文档v2.0模板（含弹性增强层）
- `references/core-setting-change-workflow.md` — 核心设定中途变更工作流（金手指/世界观更换时的6步流程）
- `references/historical-research-to-outline.md` — 历史素材→修仙大纲转化工作流（调研、映射、事件→阶段转化）
- `references/platform-strategies.md` — 网文平台运营策略（2026年）：各平台定位、推荐机制、签约门槛、AI政策、切书策略、打脸节奏对比
- `references/platform-monopoly-xianxia-playbook.md` — 修仙平台资本主义与历史大案融合创作手册：平台地租、仅退款、飞符郎零工化及晚明修坝案大纲构建方法
- `references/open-novel-fanqie.md` — 张力AI写小说全流程教学（番茄向）核心SOP及金手指账目化、人设圣经模板
- `scripts/resume_from_timeout.sh` — 超时恢复脚本，检测已写章节并生成续写起点
- `scripts/novel_pipeline.py` — 流水线核心调度工具，包含初始化、状态更新、自动校验与自动提交